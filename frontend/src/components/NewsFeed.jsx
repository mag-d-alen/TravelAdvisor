/** @format */

import React from 'react';
import { Box } from '@mui/material';
import styled from '@emotion/styled';

export default function NewsFeed() {
  return <StyledBox>news</StyledBox>;
}
const StyledBox = styled(Box)`
  background-color: violet;
  margin: 1rem;
  width: 100%;
`;
